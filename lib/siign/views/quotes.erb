<h1>Devis</h1>
<ul class="list-group">
 <% @quotes.each do |quote| %>
   <li class="list-group-item d-flex justify-content-between align-items-start">
     <div class="ms-2 me-auto">
       <span class="badge text-bg-primary"><%= quote.status %></span>
       <span class="badge text-bg-secondary"><%= quote.emission_date %></span>
       <%= quote.title %>
       <span class="badge text-bg-info"><%= quote.total_excluding_taxes %>€</span>
     </div>
     <form method="post" action="/devis/<%= quote.id %>">
       <button type="submit" class="btn btn-primary btn-sm">Faire signer le devis</button>
     </form>
   </li>
 <% end %>
</ul>